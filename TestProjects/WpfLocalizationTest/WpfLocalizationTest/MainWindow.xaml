<Window x:Class="WpfLocalizationTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        xmlns:res="clr-namespace:WpfLocalizationTest.Properties">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label Content="{x:Static res:Resources.LoginLabel}" Grid.Row="0"/>
        <Button Content="{x:Static res:Resources.ButtonContent}" Click="Button_Click" Grid.Row="1"/>
    </Grid>
</Window>
